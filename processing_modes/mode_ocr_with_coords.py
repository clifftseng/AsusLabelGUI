# Module for OCR with Coordinates mode (placeholder)

def execute(log_callback, progress_callback, pdf_path, format_path=None, verbose=False):
    log_callback("--- OCR + 座標模式 ---")
    log_callback("此功能尚未實作。")
